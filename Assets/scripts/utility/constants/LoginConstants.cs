using UnityEngine;

namespace Constant
{
    public static class LoginConstants
    {
        public enum PreLoginButton
        {
            StartLogin,
            Option,
            Logout,
            DeleteAccount
        }
        
        public enum LoginButton
        {
            Google,
            Facebook,
            Guest,
            Close
        }
    }
}

